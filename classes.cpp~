#include<iostream>
#include<cstring>
#include<vector>

#include "media.h"

using namespace std;

void ADD(vector<Media*>* media);

void ADD(vector<Media*>* media) {
  Media* media = new Media();
  cin.get(media->getTitle(), 30);
  cin.clear();
  cin.ignore(10000, '\n');
  cin >> media->getYear();
  
}

int main() {
  
